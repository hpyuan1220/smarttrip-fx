# OpenAI（用於 /api/generate 生成行程）。未設定時會自動使用內建的關西示範行程。
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# 匯率：預設使用免金鑰的即時匯率來源（fawazahmed0 currency-api，支援 TWD 等多數幣別）。
# 下方 EXCHANGE_API_KEY 為選填備援（exchangerate.host）；兩者皆失敗時才退回本機模擬資料。
EXCHANGE_API_KEY=
